# 文件通道 FileChannel —— 台式机 ⇄ 笔记本 互传文件

两台机器各开一个窗口，把文件/文件夹拖进去就发过去；对面会弹出提示，可以**接受**或**拒绝**。
不需要中转服务器，两边直连（走校园网 / Radmin VPN / Tailscale 都行）。

```
   台式机 (网线 10.20.11.15)                笔记本 (校园网 Wi-Fi)
   ┌────────────────────┐                  ┌────────────────────┐
   │  文件通道 窗口      │   ── 直连 ──►    │  文件通道 窗口      │
   │  拖文件进来 ───────┼─────────────────►│  弹窗：接受 / 拒绝   │
   │  接收目录 ◄────────┼──────────────────┤  拖文件进来          │
   └────────────────────┘                  └────────────────────┘
```

---

## 一、先让两台电脑“能互相看见”

程序本身不解决“网络通不通”，它只需要**对方的 IP 能连上**。按下面的顺序试，哪种通用哪种：

### 方案 A：同一局域网（最简单，如果笔记本也连的是校园网且能互通）
1. 两台机器都打开文件通道，窗口第一行会显示**本机地址**（例如 `10.20.11.15:8765`）。
2. 在“对方 / 连接”里填对方显示的地址，点 **测试连接**，显示“● 已连接”就通了。
3. 如果显示连不上：校园网经常把有线和 Wi-Fi 分成不同网段、并且禁止终端之间互访——
   这时用方案 B/C。

### 方案 B：Radmin VPN（台式机已经装好并在线：26.172.14.81，推荐）
1. 笔记本去 <https://www.radmin-vpn.com/> 下载安装 Radmin VPN。
2. 打开 Radmin VPN → **网络 → 加入网络**（或“创建网络”）：
   - 让两台电脑加入**同一个网络**（同一个网络名 + 密码）；
   - 台式机这边打开 Radmin VPN，创建一个网络（例如 `tfs-home`，设个密码），
     笔记本用这个网络名和密码“加入网络”。
3. 加入成功后两台机器都会得到 `26.x.x.x` 的地址。
4. 在文件通道里填对方的 `26.x.x.x:8765`，点 **测试连接**。

### 方案 C：Tailscale（台式机装了但当前没登录）
1. 两台机器都装 Tailscale，用**同一个账号**登录（<https://tailscale.com/download>）。
2. 每台机器会得到 `100.x.y.z` 的地址，在文件通道里填这个地址。
3. Tailscale 走 443/UDP，穿透校园网最稳，缺点是首次要注册账号。

> 判断用哪个：**测试连接** 能变成绿色的“● 已连接”就行。地址填在“对方地址”里。

---

## 二、平时怎么用（三步）

1. **两台机器各双击** `启动文件通道.cmd`（或直接双击 `dist\FileChannel.exe`）。
2. 第一次用的时候：
   - 在对方那台机器窗口顶部复制它的**本机地址**，填到自己窗口的“对方地址”里，点 **测试连接**；
   - 点 **记住对方** 起个名字（例如“笔记本”），以后从“常用”下拉框直接选。
3. **把文件或文件夹直接拖进蓝色区域** → 对面立刻弹窗：

   ```
   【台式机】 想给你发送文件
   共 3 个文件，合计 12.40 MB
       报告.pdf        2.10 MB
       数据.xlsx       0.30 MB
       ...
   [✓] 以后自动接受来自该主机的文件        (60 秒后自动拒绝)
        [ 接受 ]        [ 拒绝 ]
   ```

   接受后开始传输，两边都有进度；对方收完会响一声提示，文件在
   `桌面\文件通道接收\<发送方>_<时间>\` 里。

---

## 三、几个实用细节

| 功能 | 说明 |
|---|---|
| 拖文件夹 | **按原目录结构直传**：不打包、不占额外磁盘、立刻开始；空文件夹也会建出来 |
| 大文件 | 流式传输，1 MB 一块，显示百分比进度，不占内存 |
| 传输出错 | 只认“完整收到”的文件；断线/不完整会报错，不会留下半个文件当成功 |
| 口令（可选） | 在任一台窗口里把“口令”填上并发给对方，两边填一样才允许发送；校园网里建议设置 |
| 常用对方 | “记住对方”后可从下拉框直接切换；删除记录用“删除该记录” |
| 接收目录 | 可改（默认桌面\文件通道接收） |
| 自动接受 | 只有你在弹窗上勾选“以后自动接受来自该主机的文件”才会生效，默认每次都问 |

---

## 四、命令行模式（可选，方便排查/脚本化）

```bat
:: 让对方开着接收（自动接受，适合临时用）
FileChannel.exe --serve --port 8765 --auto-accept --dir D:\收件

:: 给对方发文件/文件夹
FileChannel.exe --send D:\报告.pdf D:\数据 --to 26.x.x.x:8765 --pin 你的口令
```

---

## 五、排错

| 现象 | 处理 |
|---|---|
| **测试连接**：连不上 | ① 对方程序是否开着（窗口底部日志有“正在监听 0.0.0.0:8765”）；② 地址/端口写对没有；③ 两台机器是否在同一网络（方案 A/B/C）；④ 对方第一次运行时 Windows 防火墙弹窗要点“允许访问”，或者右键运行一次 `安装防火墙规则(以管理员运行).cmd` |
| 端口被占用 | 说明已经开了一个副本；关掉旧的，或在启动时用 `--port 8766` 换端口 |
| 对方收不到提示 | 看对方窗口底部日志；若显示“收到来自…的请求”，说明通了，只是弹窗被别的窗口挡住了（窗口会强制置顶+响铃） |
| 速度慢 | 走 Radmin/Tailscale 时受中转影响；同一局域网直连最快 |
| 想清掉配置 | 删除 `C:\Users\<用户名>\.file_channel\config.json` |

---

## 六、文件清单

```
file_channel\
├── dist\FileChannel.exe          ← 独立程序（笔记本不需要装 Python）
├── 启动文件通道.cmd               ← 双击启动
├── 安装防火墙规则(以管理员运行).cmd
├── FileChannel.py                ← 源码（可选）
├── test_filechannel.py           ← 自动化测试（10 组，全部通过）
└── 使用说明.md                    ← 本文件
```

**怎么把程序弄到笔记本上**：把整个 `文件通道` 文件夹拷到 U 盘，或者用微信/网盘发过去，
笔记本上双击 `启动文件通道.cmd` 即可（绿色软件，不写注册表，除了 `%USERPROFILE%\.file_channel\config.json`）。
