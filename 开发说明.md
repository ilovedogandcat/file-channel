# 文件通道 开发/构建说明

## 结构

| 文件 | 作用 |
|---|---|
| `FileChannel.py` | 全部逻辑（HTTP 传输 + 接受/拒绝 + 拖放 + 自动发现 + 命令行模式） |
| `test_filechannel.py` | 自动化测试（10 组，含真实传输、哈希校验、口令、路径穿越、超时） |
| `dist\FileChannel.exe` | PyInstaller 打包出的独立程序（笔记本不需要装 Python） |
| `启动文件通道.cmd` | 双击启动（优先用 exe，其次用本地 Python） |
| `安装防火墙规则(以管理员运行).cmd` | 一次性放行 TCP 8765 / UDP 8766 |

## 协议（HTTP/1.1，自己实现服务端）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/ping` | 握手/连接测试：返回 app、名称、主机名、端口、是否需要口令 |
| POST | `/offer` | 发送方提交文件清单（JSON：`from`、`files[{name,size}]`），**阻塞等待对方点接受/拒绝**，超时（接收方弹窗 60 秒）自动拒绝；接受则返回一次性 `token` |
| POST | `/upload` | 携带 `X-Token` / `X-File-Name` / `Content-Length`，原始字节流；服务端流式落盘（1 MiB 一块），校验字节数完整才返回 `ok:true` |
| 口令 | `X-Pin` 头 | 服务端配置了口令就必须匹配，否则 403 |

安全细节：
* 文件名经过 `safe_name()` 净化（去路径、去非法字符），只允许落在接收目录内；
* `token` 由 `secrets.token_urlsafe` 生成，30 分钟过期，没有 token 不能上传；
* 传输不完整（字节数不符）返回错误并报日志，不会当成成功；
* 默认每次都要人工“接受/拒绝”，只有勾选“以后自动接受该主机”才走白名单。

自动发现：UDP 8766 广播（每个网卡算广播地址，含 Radmin 的 `26.255.255.255`），3 秒一次心跳；
收到同样跑 FileChannel 的机器就填进“发现”下拉框。校园网跨网段通常收不到广播，用 IP 手动填即可。

拖放：纯 `ctypes` 调用 `DragAcceptFiles` + 子类化窗口过程处理 `WM_DROPFILES`（不依赖 tkinterdnd2）；
`simulate_drop()` 可合成 `WM_DROPFILES` 消息做自动化测试，见 `--selftest-drop`。

## 构建 / 测试

```bat
:: 跑测试（不需要人工点击）
python test_filechannel.py

:: 打包
python -m PyInstaller --noconfirm --clean --onefile --windowed --name FileChannel FileChannel.py

:: 打包后的自检：GUI 拖放
dist\FileChannel.exe --selftest-drop out.json 文件1 文件2
```

注意：PyInstaller `--onefile` 会先起一个父进程（bootloader）再起真正的子进程，
**主窗口属于子进程**，用 `Get-Process` 看父进程会以为“没有窗口”。

## 已知边界

* 单文件上限受磁盘/网络限制，无硬编码上限；文件夹会自动压成 zip（大文件夹打包时日志里有提示）。
* 断点续传没有实现：中断需要重发（完整性问题会被发现，不会静默出错）。
* 传输是明文 HTTP，只建议在局域网/VPN 内使用；需要更强隔离就设置口令。
* 只做文件传输，不含聊天/剪贴板同步（如需要可以在此协议上加 `/text`）。
